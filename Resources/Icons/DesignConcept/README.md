# アイコンデザインコンセプト

## 技術仕様

### アイコンファイル規格
- **解像度**: 96×96 ピクセル
- **DPI メタデータ**: 288 DPI（PNG の pHYs チャンク）
- **WPF 論理サイズ**: 96 ÷ (288 ÷ 96) = **32 論理ピクセル**（リボンボタン枠にちょうど収まる）
- **ファイル名規則**: `{name}_96.png`
- **フォーマット**: PNG（透過背景）

### 描画スペースの考え方
- 32×32 の論理座標系で設計し、`s(v) = v * 3` で 96px にスケール
- 座標・サイズはすべて `s()` 関数を通す（例: `s(16)` = 中央）

### 生成ツール
- **メインツール**: `Tools/IconGenerator96.html`（ブラウザで開いてプレビュー＆ダウンロード）
- **DPI 設定**: ツール内で pHYs チャンクを自動付与（288 DPI）
- **フォールバック**: Python Pillow（`dpi=(288.0106, 288.0106)` を指定）

---

## カラーパレット（共通）

| 用途 | 変数名 | カラーコード |
|------|--------|-------------|
| 最暗色（輪郭・強調） | `DARK` | `#323232` |
| 中間色（図枠・補助線） | `MID` | `#5C5C5C` |
| 薄色（背面シート） | `LIGHT` | `#B0B0B0` |
| アクセント青（ビューポート） | `BLUE` | `#4A90D9` |
| Excel 緑 | `GREEN` | `#217346` |
| オレンジ（クロップボックス） | `ORANGE` | `#FF9900` |
| ピンク（色分けパレット） | `PINK` | `rgb(255,128,148)` |
| 黄（色分けパレット） | `YELLOW` | `rgb(218,185,47)` |
| 青（色分けパレット） | `BLUE_PAL` | `rgb(30,144,255)` |

---

## シート表現の設計ルール（sheet_creation / viewport 系）

シートを表現するすべてのアイコンで統一するルール。

### 縦横比
- **A版用紙の縦横比（√2:1）に準拠**
- 32 座標系での標準サイズ: **幅 26 × 高さ 18**（= 1.44:1 ≈ √2）

### 線の太さ（統一）
- シート外枠線・図枠線・表題欄線： すべて **`s(0.6)`** で統一
- コピー/ペーストの区別用ビューポート線のみ **`s(1.2)`**

### 図枠線
- シート外枠の内側 2px 内側に配置
- 色: `MID (#5C5C5C)`、太さ: `s(0.6)`

### 表題欄（右下の小さい四角）
- 図枠の右下角に配置（図枠の右端・下端と一致）
- サイズ: 幅 8 × 高さ 3 程度
- 色: `MID`（図枠線と同色）、太さ: `s(0.6)`（図枠線と同太さ）

### コピー vs ペーストの区別
- **コピー**: 破線（`setLineDash([s(2.5), s(1.5)])`）
- **ペースト**: 実線
- 両方とも線幅は同じ `s(1.2)`

---

## 各アイコンのデザイン概要

### 通り芯・レベル パネル

#### `both`（両端表示）
- 2本の縦線 + 2本の横線（格子状）
- 各交点にバブル（白丸、黒縁 R=3）
- 線はバブルの縁から縁まで（はみ出さない）

#### `left`（左端のみ）
- 上2つ + 左2つのバブル（合計4個）
- 線の右端・下端はバブルなしで canvas 端まで伸ばす

#### `right`（右端のみ）
- 上2つ + 右2つのバブル（合計4個）
- 線の左端・下端はバブルなしで canvas 端まで伸ばす

---

### シート パネル

#### `sheet_creation`（シート一括作成）
- **3枚のシートが斜め方向に重なる**表現
- 奥から手前へ: LIGHT → MID → DARK の線色で奥行き感
- 最前面シートのみ図枠線 + 表題欄を描画
- シート配置（各 26×18）: 3枚目(6,5)、2枚目(3,8)、1枚目(0.5,11)

---

### ビュー パネル

#### `viewport_copy`（ビューポート位置コピー）
- シート1枚（A版比率）+ 図枠線 + 表題欄
- ビューポートを表す **青色破線の四角**（シート左寄り）
- 破線: `[s(2.5), s(1.5)]`

#### `viewport_paste`（ビューポート位置ペースト）
- シート1枚（A版比率）+ 図枠線 + 表題欄
- ビューポートを表す **青色実線の四角**（位置・サイズはコピーと同じ）

#### `cropbox_copy`（トリミング領域コピー）
- オレンジの四角枠 + 8点のハンドル（四隅 + 辺中点）

#### `cropbox_paste`（トリミング領域ペースト）
- 青の四角枠 + 8点のハンドル（位置・構成はコピーと同じ）

#### `view_copy`（3Dビューコピー）
- アイソメトリック立方体（淡い青系3色）、6頂点定義（Vt/Vtr/Vtl/Vc/Vbr/Vbl/Vb）
- 面: 上面 `#E0EEF8`、左面 `#B0CCE0`、右面 `#88B0CC`、辺: `#2A4A6A`
- 立方体範囲: cx=16, cy=15, dx=12, dy=7（y=1〜29, x=4〜28）

#### `view_paste`（3Dビューペースト）
- アイソメトリック立方体（濃い青系3色）、同じ6頂点定義
- 面: 上面 `#5FA8D8`、左面 `#2A7AB8`、右面 `#145890`、辺: `#0A2040`

#### `sectionbox_copy`（セクションボックスコピー）
- **`both`（通り芯両端）パターンをベース** に青破線アイソメ立方体ワイヤーフレームを重ねる
- 描画順: ①グリッド線(DARK) → ②青破線9辺立方体枠 → ③バブル（上に描画）
- 立方体: cx=16, cy=15, dx=11, dy=7、破線 `[2.5, 1.5]`、線幅 `s(1.5)`

#### `sectionbox_paste`（セクションボックスペースト）
- **`both`パターンをベース** に青実線アイソメ立方体ワイヤーフレームを重ねる
- 描画順: ①グリッド線(DARK) → ②青実線9辺立方体枠 → ③バブル
- 立方体: 同じパラメータ、実線、線幅 `s(2.0)`

---

### 注釈・詳細 パネル

#### `room_tag`（部屋タグ一括配置）
- 表形式アイコン（罫線グリッド）
- 上部1行は横結合（通し行）、下部は3列グリッド

#### `filled_region`（塗潰し領域 分割・統合）
- 左右2つのハッチング矩形
- 下部に「←→」の矢印（分割・統合を表現）

---

### 構造 パネル

#### `beam_under_level`（梁下端色分け）
- I型梁（上部）+ 上向き矢印 + ∇FL線（下部）+ 右側3色ブロック
- 3色: ピンク / 黄 / 青

#### `beam_top_level`（梁天端色分け）
- ∇FL線（上部）+ 下向き矢印 + I型梁（下部）+ 右側3色ブロック
- 3色: ピンク / 黄 / 青

#### `fire_protection`（耐火被覆色分け）
- I型梁 + グレー塗潰し被覆 + 右側3色ブロック

#### `formwork`（型枠数量算出）
- 構造要素のアウトライン + 型枠面の可視化表現

---

### データ パネル

#### `excel_export`（EXCELエクスポート）
- 左側: Excel アイコン（緑の台形 + X マーク）
- 右側: **上向き矢印**（青）= データを出す

#### `excel_import`（EXCELインポート）
- 左側: Excel アイコン（緑の台形 + X マーク）
- 右側: **下向き矢印**（青）= データを受け取る

---

## 作業進捗状況

### 完了済み（HTMLプリセット確定・96px PNG配置済み）
- [x] `both` — 通り芯両端
- [x] `left` — 通り芯左端
- [x] `right` — 通り芯右端
- [x] `sheet_creation` — シート一括作成（A版比率・表題欄追加）
- [x] `viewport_copy` — ビューポート位置コピー（A版比率・表題欄・破線）
- [x] `viewport_paste` — ビューポート位置ペースト（A版比率・表題欄・実線）
- [x] `view_copy` — 3Dビューコピー（アイソメ立方体・淡色）
- [x] `view_paste` — 3Dビューペースト（アイソメ立方体・濃色）
- [x] `sectionbox_copy` — セクションボックスコピー（破線枠＋小立方体）
- [x] `sectionbox_paste` — セクションボックスペースト（実線枠＋小立方体）
- [x] `cropbox_copy` — トリミング領域コピー（青実線＋白丸ハンドル）
- [x] `cropbox_paste` — トリミング領域ペースト（青実線＋塗潰し丸ハンドル）
- [x] `room_tag` — 部屋タグ一括配置（表形式グリッド）
- [x] `filled_region` — 塗潰し領域 分割・統合（ハッチング矩形＋矢印）
- [x] `beam_under_level` — 梁下端色分け（I梁＋上矢印＋FL線＋3色）
- [x] `beam_top_level` — 梁天端色分け（FL線＋下矢印＋I梁＋3色）
- [x] `excel_export` — EXCELエクスポート（Excelアイコン＋上矢印）
- [x] `excel_import` — EXCELインポート（Excelアイコン＋下矢印）
- [x] `fire_protection` — 耐火被覆色分け（外周矩形＋I梁断面＋3色）
- [x] `formwork` — 型枠数量算出（コンクリート面＋寸法矢印＋3色）

### 修正履歴
- `cropbox_copy`: オレンジ+四角ハンドル → 青+白丸ハンドル（元画像に合わせて修正）
- `cropbox_paste`: 四角ハンドル → 塗潰し丸ハンドル（元画像に合わせて修正）
- `fire_protection`: HTMLプリセット追加（外周矩形＋I梁断面＋グレー被覆＋3色ブロック）
- `formwork`: HTMLプリセット追加（コンクリート面＋双方向矢印＋3色ブロック）
- `view_copy/paste`: 頂点定義を6頂点方式（Vt/Vtr/Vtl/Vc/Vbr/Vbl/Vb）に統一、比率・色を改善
- `sectionbox_copy`: 平面正方形枠 → `both`パターン＋青破線アイソメ立方体9辺枠に刷新
- `sectionbox_paste`: 平面正方形枠 → `both`パターン＋青実線アイソメ立方体9辺枠に刷新

### PNG 書き出し・配置が必要（デザイン確定後）
全アイコンのプレビュー確認完了後、以下を実施:
1. `IconGenerator96.html` で各アイコンをダウンロード（DPI=288 自動付与）
2. `Resources/Icons/{name}_96.png` として配置
3. `Application.cs` でのロードを確認
4. コミット＆プッシュ

---

## 次セッションでの再開手順

1. ブラウザで `Tools/IconGenerator96.html` を開く
2. 「未レビュー」のアイコンを上から順にプルダウンで選択してプレビュー確認
3. デザインの修正が必要な場合は、このセッションのようにプリセットを編集
4. 全アイコン確認完了後、PNG を書き出して `Resources/Icons/` に配置
