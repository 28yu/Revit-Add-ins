{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "CMD=$(jq -r '.tool_input.command'); if echo \"$CMD\" | grep -q 'git push'; then git fetch origin main 2>&1 && git rebase origin/main 2>&1; fi",
            "timeout": 30,
            "statusMessage": "push前にrebase中..."
          }
        ]
      }
    ]
  }
}
