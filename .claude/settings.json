{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if echo $(jq -r '.tool_input.command') | grep -q 'git push'; then git fetch origin main 2>/dev/null; git stash -q 2>/dev/null; git rebase origin/main 2>/dev/null; git stash pop -q 2>/dev/null; true; fi",
            "timeout": 30,
            "statusMessage": "push前にrebase中..."
          }
        ]
      }
    ]
  }
}
